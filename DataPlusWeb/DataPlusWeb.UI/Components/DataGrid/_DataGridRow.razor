@typeparam TItem
@inherits DataGridComponentBase<TItem>

@if (Columns is not null)
{
    <tr @onclick="@OnClicked">
        @foreach (var column in Columns)
        {
            <_DataGridCell TItem="TItem" Row="@this" Column="@column" Item="Item" />
        }
    </tr>
}